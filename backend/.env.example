# Database Configuration
DATABASE_URL=your_postgresql_connection_string

# Authentication
JWT_SECRET=your_jwt_secret_key

# Server Configuration
PORT=3001

# Google AI Configuration
GOOGLE_API_KEY=your_google_ai_api_key_here

# Notes:
# - This key is used by the backend to call Google GenAI (Gemini) for CV parsing and other AI features.
# - Do NOT commit your real API key to version control. Use this file as a template only.
# - To enable AI features locally, copy this file to `.env` and replace the placeholder with your actual key.